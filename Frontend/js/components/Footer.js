
/**
 * COMPONENTE FOOTER
 * 
 * Footer para todas las páginas públicas.
 */

const Footer = {
  /**
   * Renderiza el footer
   * @returns {string} HTML del footer
   */
  render() {
    const currentYear = new Date().getFullYear();
    return `
     
    `;
  }
};

// Exportar
if (typeof module !== 'undefined' && module.exports) {
  module.exports = Footer;
}

